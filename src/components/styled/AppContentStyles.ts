import styled from "styled-components";

export const AppWrapper = styled.div`
  padding: 2rem;
  max-width: 1200px;
  margin: auto;
`;

export const Header = styled.div`
  text-align: center;
  margin-bottom: 1rem;
`;

export const FavoritesBadge = styled.div`
  background: #ff6b6b;
  color: white;
  padding: 0.5rem 1rem;
  border-radius: 20px;
  display: inline-block;
  margin-top: 0.5rem;
  font-size: 0.9rem;
  font-weight: 500;
`;
